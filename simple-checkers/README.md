This is just a simple checkers prototype!

The backend server will need to be converted to Go with proper distributed features.

The frontend may need to be converted to another framework for better organization and support of
additional features we wish to add (logins, spectating, etc.)

Run instructions:

- cd simple-checkers
- node server.js
- open multiple tabs of localhost:3000