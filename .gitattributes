# Force LF instead of CRLF for shell scripts
*.sh text eol=lf

# Track the large presentation video file with Git LFS
documents/checkered-10-minute-presentation.mp4 filter=lfs diff=lfs merge=lfs -text
